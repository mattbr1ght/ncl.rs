services:
    next-server:
        build:
            # context: './docker/next'
            context: './'
            # image: './docker/next'
            dockerfile: Dockerfile
        ports:
            - '${APP_PORT:-80}:3000'
